import { FilterInputPipe } from './filter-input.pipe';

describe('FilterInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInputPipe();
    expect(pipe).toBeTruthy();
  });
});
